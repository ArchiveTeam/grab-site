os:
  - linux
  - osx

sudo: false

language: python
python:
  - 3.4

install:
  - travis_retry pip3 install -q .

script:
  - ~/.local/bin/grab-site --help
  - ~/.local/bin/gs-dump-urls --help
  - ~/.local/bin/grab-site --1 https://github.com/ludios/grab-site
